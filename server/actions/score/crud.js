module.exports = (api) => {
    const Score = api.models.Score;

    function create(req, res, next) {

    }

    function update(req, res, next) {

    }

    return {
        create,
        update
    };
}
