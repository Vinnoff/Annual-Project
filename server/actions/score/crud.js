module.exports = (api) => {
    const Score = api.models.Score;

    function create(res, req, next) {

    }

    function update(res, req, next) {

    }

    return {
        create,
        update
    };
}
